# Use the bundled realistic demo dataset instead of live Chrome tabs.
VITE_USE_MOCK_ANALYSIS=false

# OpenAI-compatible endpoint. The provider origin is requested only when used.
LLM_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=
LLM_MODEL=inclusionai/ling-3.0-flash-fin:free
